# main.py
# ============================================================
# Лабораторна робота №10
# Методи Рунге-Кутта та прогнозу-корекції Адамса
# для розв'язку задачі Коші для ЗДР першого порядку
#
# Запуск: python main.py
# ============================================================
#
# СТРУКТУРА ПРОЕКТУ:
#   exact_solution.py    — параметри задачі, f(x,y), y_exact(x)
#   part1_adams.py       — Ч.1: Метод Адамса 2-го порядку (кроки 1-5)
#   part2_runge_kutta.py — Ч.2: Метод РК-4 (кроки 6-9)
#   main.py              — цей файл, запускає обидві частини
#
# РІВНЯННЯ (підставте своє з варіанту у exact_solution.py):
#   y' = -2y + x,   y(0) = 1,   x ∈ [0, 1]
#   Точний розв'язок: y(x) = x/2 - 1/4 + 5/4 * exp(-2x)
# ============================================================

import matplotlib
matplotlib.use('Agg')   # зберігати в файл без GUI (прибрати для показу)

from exact_solution import X0, Y0, X_END, H, EPS
from part1_adams      import plot_part1
from part2_runge_kutta import plot_part2

if __name__ == "__main__":
    print("╔══════════════════════════════════════════════════════╗")
    print("║   Лабораторна робота №10 — Чисельні методи ЗДР      ║")
    print("╚══════════════════════════════════════════════════════╝")
    print(f"\nПараметри задачі:")
    print(f"  Відрізок:          [{X0}, {X_END}]")
    print(f"  Початкова умова:   y({X0}) = {Y0}")
    print(f"  Крок сітки:        h = {H}")
    print(f"  Задана точність:   ε = {EPS}")
    print(f"  Рівняння:          y' = -2y + x")
    print(f"  Точний розв'язок:  y(x) = x/2 - 1/4 + 5/4·exp(-2x)")
    print()

    print("▶ Запуск Частини 1: Метод Адамса 2-го порядку...")
    plot_part1(show=False)

    print("\n▶ Запуск Частини 2: Метод Рунге-Кутта 4-го порядку...")
    plot_part2(show=False)

    print("\n✅ Лабораторна робота виконана!")
    print("   Збережені файли:")
    print("   • lab10_part1_adams.png")
    print("   • lab10_part2_rk4.png")